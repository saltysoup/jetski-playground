apiVersion: v1
kind: Service
metadata:
  name: vllm-inkling-nvfp4-service
  namespace: default
  labels:
    app: vllm-inkling-nvfp4
  annotations:
    cloud.google.com/neg: '{"exposed_ports":{"8000":{}}}'
spec:
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  selector:
    app: vllm-inkling-nvfp4
  type: ClusterIP
